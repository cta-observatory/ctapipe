External C Libraries
====================

This directory contains C libraries included with `ctapipe`. Note that only C
libraries without python-specific code  should be included in this directory.
Cython or C code intended for use with `ctapipe` or wrapper code should be in
the `ctapipe` source tree.
