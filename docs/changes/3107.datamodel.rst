Use ``ctao-datamodel`` for CTAO product metadata in HDF5 files, including
automatic conversion of legacy metadata and migration through ``ctapipe-merge``.
Updated the DataWriter API to configure contact information through the
contact_metadata trait.
